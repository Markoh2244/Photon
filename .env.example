# User access token from app.neutron.health (Network tab or copy from a logged-in session).
# Do not commit real tokens. This token can write prescriptions — keep it server-side only.
PHOTON_ACCESS_TOKEN=

# SPA credentials from Photon settings. Safe to expose to the browser.
NEXT_PUBLIC_PHOTON_CLIENT_ID=4kz7hFFyU6UsubKit63CuWvLTAZUYITg
NEXT_PUBLIC_PHOTON_ORG_ID=org_hszgTM3xKbdabX7G
NEXT_PUBLIC_PHOTON_REDIRECT_URI=http://localhost:3000

# Optional: enables free-form LLM tool calling. Without it, the deterministic MCP agent still works.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
